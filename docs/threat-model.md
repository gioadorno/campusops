# Threat Model

## Scope and assets

Protected assets are user identity, support-request confidentiality and integrity, authorization policy, JWT signing material, policy integrity, service state integrity, audit integrity, and gateway availability. MCP clients, models, arguments, prompt text, and HTTP input are untrusted. Phase 1 uses fictional data and local in-memory state.

| Threat                   | Attack                                                                                    | Explicit mitigations                                                                                                                                                                                                                                                              | Residual risk / next control                                                                                                                                                       |
| ------------------------ | ----------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Prompt injection         | Policy or support text tells a model to ignore instructions or invoke a write tool.       | Prompts label embedded text untrusted; tool authorization is deterministic and independent of the LLM; writes need `requests:write`; identity cannot be supplied as an argument.                                                                                                  | A permitted user/model can still choose an undesirable authorized action. Add confirmation policy and host UX for sensitive actions.                                               |
| Tool poisoning           | Malicious names/descriptions or a compromised registry misrepresent a tool.               | Tool surface is statically registered in source; annotations match behavior; descriptions do not grant authority; the server checks scopes and ownership regardless of client metadata.                                                                                           | Protect build provenance and dependency integrity with locked CI, review, signing, and SBOM controls.                                                                              |
| Data exfiltration        | A caller requests broad records or coerces output of secrets.                             | Per-user support queries; ownership checks; bounded limits; fictional data; no general query/eval/filesystem/network tools; audit excludes content; tokens are never tool results.                                                                                                | Add response classification, rate limits, anomaly alerts, and egress policy before real data.                                                                                      |
| Excessive permissions    | A broad token enables unnecessary writes or audit access.                                 | Small allow-listed scopes; separate read/write scopes; every handler checks its requirement; no implicit admin bypass.                                                                                                                                                            | Use short-lived audience-bound tokens, consent/step-up, role mapping, and periodic entitlement review.                                                                             |
| Malicious tool arguments | Oversized, malformed, or crafted strings trigger unsafe behavior.                         | Strict Zod schemas, trim/min/max constraints, enum allow lists, integer bounds, and no dynamic code, shell, path, URL, or SQL execution.                                                                                                                                          | Add request byte limits and rate limiting at production ingress.                                                                                                                   |
| Secret exposure          | JWT secret or bearer token appears in source, errors, or audit.                           | Runtime environment configuration; minimum secret length; production requires an explicit secret and rejects the development fallback; audit cannot accept arguments or tokens; normalized errors omit credentials and internal exception details; `.env` ignored.                | Production still needs managed secrets, rotation, redaction, and log scanning.                                                                                                     |
| Replay / retry           | Network retries duplicate a support request, or a caller reuses a key for different work. | Create requires a bounded user-scoped idempotency key and stores a SHA-256 fingerprint over canonical category, title, description, and severity fields; identical retries return the original record, while a changed payload conflicts; signed tokens expire.                   | In-memory keys disappear on restart. Production needs durable atomic idempotency records, retention, token replay defenses where appropriate, and a defined cancel retry contract. |
| Cross-user data access   | A caller supplies or guesses another user's request ID.                                   | No `userId` input exists; principal derives from verified JWT; list filters by principal; get and cancel independently check ownership; ownership is audited as deny/denied; missing and cross-user records return the identical normalized public error; tests cover violations. | IDs remain observable if leaked. Production should add monitoring and retain the non-enumerating response policy.                                                                  |

## Trust decisions

Authentication answers who the caller is. Authorization answers whether that principal may attempt an operation. Ownership answers whether that particular record is accessible. These are separate checks. The LLM participates in none of them.

Repository interfaces keep policy, service, and support access behind the application authorization/audit boundary. The MCP adapter cannot reach fixture data directly, including for resources. CI repeats frozen dependency installation, lint, typechecking, all tests, build, and the HTTP contract suite on pull requests and `master` pushes.

## Availability and local limitations

Schema limits constrain individual payloads, but Phase 1 has no HTTP rate limiter, concurrency budget, durable queue, or circuit breaker. The in-memory repositories and audit sink are neither durable nor horizontally consistent. These are accepted local-demo limitations, not production guarantees.

## Phase 2 AWS threats

| Threat                            | Mitigation                                                                                                                                                                                          |
| --------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Stolen OAuth access token         | Short Cognito access-token lifetime, TLS-only API, least-privilege scopes, no token logging, and revocation/credential reset procedures.                                                            |
| Incorrect scope mapping           | Closed, unit-tested external-to-internal map; unknown scopes confer nothing; application checks remain authoritative.                                                                               |
| API Gateway bypass assumptions    | Both routes require a CampusOps OAuth scope, Lambda derives identity only from authorizer claims, and malformed/missing claims fail; CampusOpsService still performs exact operation authorization. |
| Lambda direct invocation          | No public function URL; API Gateway permission is source-ARN constrained; IAM governs direct invoke.                                                                                                |
| IAM privilege escalation          | Operator-owned bootstrap state controls OIDC/deployment IAM; the deployment role cannot modify itself/provider and may pass only the exact Lambda runtime role to Lambda.                           |
| DynamoDB tenant exposure          | User queries bind GSI keys to Cognito `sub`; direct gets receive application ownership checks; table access is runtime-role only.                                                                   |
| Audit tampering                   | Separate encrypted/PITR table; runtime has PutItem only; safe immutable event shape; operators monitor write failures.                                                                              |
| Terraform state exposure          | Dedicated encrypted/versioned/private S3 bucket, native lockfile, least-privilege state paths, no outputs containing secrets.                                                                       |
| Compromised workflow or OIDC role | `dev` approvals, protected workflow review, short-lived credentials, exact repository/environment trust, and no deployment-role self-administration.                                                |
| Origin/DNS rebinding              | Exact configured origins, no wildcard, browser Origin checked in Lambda; absent Origin allowed only for non-browser MCP clients.                                                                    |
| Replay/concurrent idempotency     | Canonical SHA-256 payload binding, transactional conditional creation, consistent winner re-read, and TTL cleanup.                                                                                  |

Authentication, application authorization, record ownership, runtime IAM permissions, and deployment permissions are independent boundaries; success at one never implies success at another.
